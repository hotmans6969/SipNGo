org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.nonTransitiveRClass=true

# The deployment the app opens. Overridable at build time with
# -PsipngoUrl=... so an APK can target a different host without a code change.
sipngoUrl=https://sip-n-go.vercel.app
sipngoHost=sip-n-go.vercel.app
